<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="16dp">

    <ImageView
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:id="@+id/select_zone_item_image"
        app:srcCompat="@drawable/ic_account"
        app:layout_constraintStart_toStartOf="parent"/>

    <TextView
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:id="@+id/select_zone_item_name"
        app:layout_constraintStart_toEndOf="@id/select_zone_item_image"
        android:layout_marginStart="16dp"
        app:layout_constraintTop_toTopOf="@id/select_zone_item_image"
        app:layout_constraintBottom_toBottomOf="@id/select_zone_item_image"/>

</android.support.constraint.ConstraintLayout>