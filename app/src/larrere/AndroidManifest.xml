<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application android:label="@string/app_name"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">  <!-- TODO handle GoogleAppIndexing -->

        <activity android:name="ekylibre.zero.ObservationActivity"
            android:parentActivityName="ekylibre.zero.home.MainActivity" />

        <service android:name="ekylibre.service.SimpleLocationService" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="ekylibre.zero.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>